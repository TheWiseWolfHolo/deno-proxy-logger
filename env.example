# 上游
UPSTREAM_BASE_URL=https://wolfholo-gcli.zeabur.app
UPSTREAM_KEY=replace_me

# 访问你这层代理的 token（客户端用 Authorization: Bearer <PROXY_TOKEN>）
PROXY_TOKEN=replace_me

# 是否记录响应片段（1/0）
LOG_RESPONSE=1

# 最多记录多少字节（请求/响应都会用这个上限，避免 KV 单条过大）
MAX_LOG_BYTES=32768


